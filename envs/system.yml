name: scbolt-system
channels:
  - conda-forge
  - defaults
dependencies:
  - bash>=5
  - coreutils>=9
  - curl>=8
  - findutils>=4
  - gawk>=5
  - grep>=3
  - gzip>=1
  - make>=4
  - sed>=4
  - tar>=1
  - wget>=1
